module.exports = {
    theme: {
      // Some useful comment
      fontFamily: {
        'lora' : ['"Lora"', 'serif']// Ensure fonts with spaces have " " surrounding it.
      },
    },
    variants: {
      // Some useful comment
    },
    plugins: [
      // Some useful comment
    ]
  }